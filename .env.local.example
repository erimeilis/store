# Store - Local Development Configuration
# Copy this file to .env.local for local development
# These values override .env for local dev only

# =============================================================================
# LOCAL DEVELOPMENT OVERRIDES
# =============================================================================
# Leave domains empty for local dev - uses localhost automatically
API_DOMAIN=
FRONTEND_DOMAIN=

# Local URLs (used by dev scripts)
API_URL=http://localhost:8787
FRONTEND_URL=http://localhost:5173

# =============================================================================
# GOOGLE OAUTH (copy from .env - required for local Google login)
# =============================================================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Note: API tokens (ADMIN_ACCESS_TOKEN, FRONTEND_ACCESS_TOKEN) are managed
# by token-manager. Run: npm run db:reset:local to generate tokens.
